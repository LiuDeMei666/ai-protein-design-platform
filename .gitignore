# ==========================================================================
# AI 辅助蛋白设计平台 —— Git 排除清单
# --------------------------------------------------------------------------
# 划分原则只有一条：
#
#   源码、以及**离线环境下没法现拉**的参考数据  -> 进仓库
#   运行时状态、可再生的产物、体积巨大的权重     -> 不进仓库，由脚本生成
#
# 本项目的终点是企业内网离线交付，所以 frontend/vendor（第三方 JS 库）
# 与 data/seeds（参考序列 + 密码子表）必须随仓库走；而模型权重、SQLite 库、
# 缓存则由 scripts/ 下的脚本或程序启动时生成。
# ==========================================================================

# --------------------------------------------------------------------------
# 模型权重（约 5.2 GB）—— 绝不入库
# --------------------------------------------------------------------------
# 获取方式：python scripts/download_esm2.py
#
# 为什么必须排除，而不只是"仓库大"：
#   1) GitHub 单文件上限 100 MB，直接推不上去；
#   2) 一旦进过历史，即使之后删除，这 5.2 GB 仍永久留在 .git 里，
#      任何人 clone 都要拖下来，只能靠重写历史才能清掉；
#   3) 权重是可再生的（有下载脚本），没有版本化价值。
models/

# --------------------------------------------------------------------------
# 运行时状态 —— 绝不入库
# --------------------------------------------------------------------------
# SQLite 业务库：二进制文件无法自动合并，两个人同时跑过服务就会冲突；
# 更关键的是它承载企业真实录入的数据，不应进入代码仓库。
data/db/
# 结构预测结果与嵌入缓存：纯缓存，删掉会自动重建
data/cache/
# 用户上传的 FASTA 等文件
data/uploads/

# --------------------------------------------------------------------------
# 可再生的验证产物
# --------------------------------------------------------------------------
# 注意边界：data/validation/ 下的 case_*.json 是**验收基准快照**，要入库
# （用来判断"这次改动有没有改变结果"）；但 ui/ 下是 scripts/verify_ui.py
# 生成的截图，属于二进制且每次重跑都会变，不入库。
data/validation/ui/

# --------------------------------------------------------------------------
# 日志
# --------------------------------------------------------------------------
# logs/ 目录本身保留（见 logs/.gitkeep）——run_server.sh 或重定向写入
# logs/server.log 时，目录不存在会直接报错，而目录是程序启动后才创建的。
logs/*
!logs/.gitkeep

# --------------------------------------------------------------------------
# Python
# --------------------------------------------------------------------------
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/
*.egg-info/
build/
dist/

# --------------------------------------------------------------------------
# 虚拟环境
# --------------------------------------------------------------------------
.venv/
venv/
env/
ENV/

# --------------------------------------------------------------------------
# 配置与密钥
# --------------------------------------------------------------------------
# 只提交 .env.example 作为模板；真实 .env 含机器相关路径与可选 API Key
.env
.env.local
*.pem
*.key
!requirements.txt

# --------------------------------------------------------------------------
# 编辑器 / 操作系统
# --------------------------------------------------------------------------
.vscode/
.idea/
.DS_Store
Thumbs.db
*.swp
*~

# --------------------------------------------------------------------------
# 交付产物（由脚本打包，不入库）
# --------------------------------------------------------------------------
release/
release-*.tar.gz
